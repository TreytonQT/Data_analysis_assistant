# 命令执行约束

在本项目中运行安装、构建或测试命令时，必须遵守以下规则：

1. 单条命令的超时时间不得超过 90 秒；达到 90 秒立即停止该命令。
2. 不得无限等待，也不得对同一失败命令进行无上限重复尝试。
3. 安装命令失败后最多只允许重试一次；第二次失败后停止安装并记录原因。
4. 命令超时或失败后，先向用户说明原因，再继续完成不依赖该命令的代码修改和检查。
5. 大批量依赖必须拆成多个小批次安装，每个批次仍受 90 秒和一次重试限制。

这些限制同样适用于子项目（包括 `frontend/`）及其脚本。
