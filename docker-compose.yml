services:
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./data:/app/data
      - ./configs:/app/configs
    init: true
    restart: unless-stopped
