# 更新记录

本项目从 `v2.0.1` 起按 GitHub Release 版本记录用户可见变化。

## v2.1.0 - 2026-09-04

### 新增

- 新增多级业务导航、最近访问标签页和浏览器历史恢复；页面切换可保留当前筛选条件，并按数据域刷新受影响的页面。
- 新增本机系统控制：源码桌面启动器和 Windows 便携版支持在页面内安全重启或退出服务；源码部署可安装桌面“销售数据看板”快捷方式。
- 部门监控新增考核视图，按月份、开发员和店铺汇总中企/本土销售额，并可在页面调整本土业绩折扣比例计算考核销售额。
- 上传中心新增 SKU 图片映射表，产品、滞销和补货矩阵可展示并预览库存图片；上传前校验 HTTPS 链接、重复映射和关联情况。
- 产品矩阵新增当前促销折扣；滞销矩阵新增日均销量与最近一次促销日期、折扣快照，相关字段同时进入完整 CSV 导出。

### 优化

- 统一上传、配置、促销、待办、复制和下载操作的进行中、成功、警告与失败反馈，页面加载失败时保留可恢复的错误提示。
- 补货管理默认最低补货量调整为 30，支持选择全部匹配开发员、展示 ASIN 图片，并要求填写关闭补货原因。
- 经营、产品、低毛利和滞销决策矩阵优化字段分组、数值格式、Rating/广告占比状态及服务端排序。
- 促销记录删除后保留 SKU 最近一次促销快照；批次、上传、配置和业务页面在数据变更后执行更精确的局部刷新。
- 源码启动改为无终端依赖的后台进程，统一 PID 与日志管理；重启时等待旧端口释放后再启动替代进程。

### 修复与数据安全

- 修复同一 SKU 多开发员导致的产品行重复、全开发员销量在售数口径、滞销日均销量缺失/零值处理，以及零销量空 ASIN 行阻塞月度销量替换的问题。
- 强化报表预览、下载、替换和删除的路径边界检查，图片映射冲突与无效链接不会覆盖现有数据。
- 默认目标、提成、部门费用率和补货开关配置同步到当前业务值。

### 数据与升级

- 升级前请停止旧程序并备份 `data/` 与 `configs/`；本版本新增的 `sku_image_map` 数据源保存在 `data/`，需随业务数据一起迁移。
- 便携版仍不包含业务数据、上传文件、SQLite 数据库或缓存；首次启动会创建空数据目录。

## v2.0.1 - 2026-08-10

### 新增

- 使用 React + FastAPI 重建桌面端应用，替代原 Streamlit 界面。
- 新增批次监控：批次创建/导入、美工图、上架售价、货件绑定、到货状态和异常 SKU 清单。
- 重构补货管理为 ASIN 决策矩阵，加入德/法/西/意站点毛利、趋势测算、滚动 12 个月销量画像、SKU 明细、产品标签、补货开关和 Excel 导出。
- 新增部门监控及销量/销售额明细数据源，展示在售 SKU、库存总数、占用资金、订单与销售额。
- 上传中心按每日、每周、每月分组，并支持往月销量的连续 12 个月滚动维护。
- 新增 Windows x64 便携版及 GitHub Actions 自动测试、打包、校验和 Release 发布流程。

### 优化

- 产品、滞销与促销功能补充多源数据联动、搜索、分页、排序和导出能力。
- 补货 SKU 明细展示四站点毛利率，零评价 Rating 按缺失状态展示。
- 上传、配置、报表替换和缓存流程增加格式校验、回滚与数据安全保护。
- React 页面采用按需加载和域级刷新，FastAPI 响应支持 GZip 与静态资源缓存。

### 数据与升级

- 便携版不包含业务数据、上传文件、SQLite 数据库或缓存。
- 从旧版本升级前必须停止程序并备份 `data/` 和 `configs/`；`data/cache/` 可重建，无需迁移。
